server:
  port: 8080

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devsouzx/Letterboxd-Clone-Microservicos-Repository
          default-label: main